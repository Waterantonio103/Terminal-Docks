# Documentation Index

Start with current architecture docs:

- `AGENTS.md`: repo-specific rules for AI coding agents.
- `architecture.md`: ownership boundaries and target layering.
- `repository-map.md`: source tree navigation for implementation work.
- `control-planes.md`: desktop UI, backend JSON-RPC, CLI wrapper, and MCP ops.
- `provider-adapters.md`: provider adapter contract and test expectations.
- `persistence-ownership.md`: canonical database/schema ownership.
- `mcp-tool-contracts.md`: stable agent-facing MCP tool envelopes.

Planning and historical notes:

- `cao-quality-refactor-agent-guide.md`: quality refactor direction inspired by CAO.
- `workflow-runtime-plan.md`: historical workflow runtime implementation plan.
- `runtime-ui-mcp-plan.md`: historical runtime UI and MCP integration plan.
- `workflow-execution-fix.md`: historical Tauri-era execution debugging notes.
- `plan.md`: older multi-agent improvement plan.
- `agents.md` and `claude.md`: short pointers for agent-specific entry points.

Historical docs are reference material only. Current setup, commands, and
architecture boundaries live in `README.md` and the current architecture docs
listed above.
