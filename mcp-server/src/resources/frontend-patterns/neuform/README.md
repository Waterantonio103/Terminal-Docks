# Neuform Frontend Patterns

This directory contains optional, non-mandatory frontend pattern resources derived from the public Neuform prompt skill catalog. They are intended as a heavy-recommend pattern library for visual-first work and as a future data source for a theme-picker `Effects` section.

Use `index.json` for discovery. Load at most 1-3 matching patterns for a task. Accepted project specs override these patterns.

Generated entries: 69.
